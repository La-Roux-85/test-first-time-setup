# load libraries
library(usethis)
library(gitcreds)
library(credentials)
library(gert)
library(gh)

# add credentials
# use_git_config(user.name = "Jane Dow", user.email = "YourEmail@xxx.xxx")

# Set - up personal access token (run line below if you need to get a token)
# create_github_token()

# # Run this to set token
# gitcreds_set()